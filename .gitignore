# Node.js specific ignores
node_modules/
/npm-debug.log*
/yarn-debug.log*
/yarn-error.log*
/pnpm-debug.log*
.npm
.eslintcache

# Environment variables
.env
.env.local

# Electron-specific build and packaging ignores
# Directories created by build tools like Electron Forge, Electron Packager, etc.
/dist
/out
/release
built/
bundles/

# macOS ignores (often generated by the OS)
.DS_Store

# Windows ignores
Thumbs.db
Desktop.ini

# Linux ignores
# (no common specific files that aren't already covered)

# Optional: if you use a specific IDE, add its generated files here.
# Example for VS Code:
.vscode/